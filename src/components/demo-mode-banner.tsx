/**
 * src/components/demo-mode-banner.tsx
 *
 * Legacy export — delegates to DemoCompactBanner so existing import sites
 * (chase-today, dashboard-view) get the updated design automatically.
 */

export { DemoCompactBanner as DemoModeBanner } from "@/components/demo-banner";
